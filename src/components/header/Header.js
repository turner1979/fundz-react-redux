import React from 'react';
import './Header.scss';
import Logo from '../logo/Logo';

function Header() {
  return (
    <div className="header">
      <Logo />
    </div>
  );
}

export default Header;